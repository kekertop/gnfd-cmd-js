//delete if not needed
interface mainInterface {
    host: string;
    rpcAddr: string;
    chainId: string;
    passwordFileFlag: string;
    config: string;
    keystore: string;
}

function main(params: mainInterface) {

}